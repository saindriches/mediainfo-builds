# The MediaInfoLib source uses CRLF; format-patch preserved CRLF in the diff bodies, so the .patch
# files must be stored byte-exact or they will not apply. Keep git from normalizing line endings.
patches/**/*.patch -text
